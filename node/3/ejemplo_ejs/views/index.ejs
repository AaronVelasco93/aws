<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>lista de mis usuarios</title>
</head>
<body>

    <h1>Usuarios</h1>
    <a href="/create">Creadr un nuevo ususario</a>
    <ul>
        <% users.forEach( user=>{ %>
            <li>
                <%= user.name %> - <%= user.age  %> años
                <a href="/edit/<%= user._id %>">Editar</a>
                <a href="/delete/<%= user._id%>">Eliminar</a>
            </li>
            <% }) %>
    </ul>
    
</body>
</html>